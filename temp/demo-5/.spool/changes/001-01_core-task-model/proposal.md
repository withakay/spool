# Proposal

## Why

- Establish a shared task model so storage and CLI can agree on format.

## What Changes

- Add `Task` struct and helpers to format/parse line-based storage.

## Impact

- Adds `src/core.rs` and updates `src/lib.rs`.
