This directory contains embedded instruction templates.

- File format: Markdown templates rendered with minijinja (`*.md.j2`).
- These templates are embedded into the Rust binaries via `spool-templates`.
