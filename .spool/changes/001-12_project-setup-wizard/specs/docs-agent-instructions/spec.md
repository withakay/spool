## ADDED Requirements

### Requirement: Docs mention project setup workflow

AI-facing documentation installed by Spool SHALL mention how to run the project setup workflow.

#### Scenario: Docs include command to run project setup

- **WHEN** a user reads installed agent docs
- **THEN** they can find `/spool-project-setup` or `spool agent instruction project-setup`
